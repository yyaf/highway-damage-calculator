# 高速公路路产损害赔偿金额计算器 — 项目规划与开发总结

## 一、项目概述

基于广东省两份官方赔偿标准（粤交路〔1998〕38号 及 粤公路函〔2020〕352号增补），构建微信小程序计算器。纯前端、无后端、无第三方依赖，数据硬编码，历史记录用 wx.Storage。

- **AppID**: `wx2e770f03e3a84dc6`
- **作者**: 岳亚峰
- **版本**: v1.0.0

---

## 二、技术架构

### 技术选型

| 项目 | 选择 |
|------|------|
| 框架 | 微信小程序原生（WXML + WXSS + JS） |
| 依赖 | 无第三方依赖 |
| 持久化 | wx.Storage（本地存储） |
| 图标 | Node.js 生成 PNG（zlib + raw RGBA buffer） |

### 项目结构

```
├── app.js                          # 入口：初始化 Storage
├── app.json                        # 全局配置 + tabBar + lazyCodeLoading
├── app.wxss                        # 全局样式变量（--primary, --danger, --border）
├── data/
│   └── prices.js                   # 赔偿标准数据（~160项，14大类，合并两份标准）
├── utils/
│   ├── calculator.js               # 购物车计算 + 文本报告 + 中文大写金额
│   ├── usage-tracker.js            # 使用频次追踪
│   └── history.js                  # 历史记录读写（最多50条）
├── pages/
│   ├── calculator/                 # Tab1 计算：搜索浮层 + 已选清单 + 底部合计栏
│   ├── standards/                  # Tab2 标准：两份标准文件卡片列表
│   ├── standard-items/             # 标准详情：只读浏览某标准的分类和项目
│   └── profile/                    # Tab3 我的：历史记录 + 版本 + 作者
└── images/                         # tabBar 图标（6张 81×81 PNG）
```

### 页面架构（三 Tab）

```
tabBar
├── 计算 (calculator)      — 搜索 + 已选清单 + 合计 + 复制清单
├── 标准 (standards)       — 两份标准文件预览
└── 我的 (profile)         — 历史记录 + 版本信息 + 作者
```

---

## 三、数据模型

### prices.js 导出两个结构

**1. `categories[]` — 合并后分类列表（供计算器主页使用）**

```js
{
  categories: [
    {
      id: 'signage',
      name: '标志牌',
      items: [
        {
          id: 'tri-130-high',        // 唯一标识
          name: '三角形反光标志牌',
          spec: '边长130cm 高强级',   // 规格描述
          unit: '块',                // 单位
          price: 2120,               // 单价（0=按实计算）
          source: '1998'             // '1998' | '1999' | '2020'
        }
      ]
    }
  ]
}
```

**2. `standards[]` — 按来源分组原始数据（供标准页只读浏览）**

```js
{
  standards: [
    { id: '1998', name: '损坏公路路产赔偿标准', docCode: '粤交路〔1998〕38号', categories: [...] },
    { id: '2020', name: '广东省损坏公路路产赔偿指导标准', docCode: '粤公路函〔2020〕352号', categories: [...] }
  ]
}
```

### 合并规则

- 同名项目在不同标准中分别以独立条目存在，各自标记来源（`source: '1998'` / `source: '2020'`）
- 仅在一方出现的保留原价格
- "按实计算"项目 `price: 0`，不参与金额计算

### 14 大分类

标志牌 → 标志牌支柱及基础 → 示警桩与视线诱导 → 路面标线及突起路标 → 路面工程 → 路基与排水防护 → 波形梁护栏 → 防撞护栏与隔离设施 → 桥梁护栏及设施 → 其他交安设施 → 机电设施 → ETC门架及防雷通信 → 声屏障与公路绿化 → 充电桩设施 → 其他公路损害

---

## 四、核心数据流

```
用户操作
  ├── onSearchFocus / onToggleDropdown → openDropdown()
  │     └── usage.sortByUsage(allItems) → 按使用频次降序排列
  ├── onSearchInput(keyword)
  │     └── 匹配 name + spec → usage.sortByUsage(matched) → 实时过滤
  ├── onQuickAdd(itemId) / onIncrease(itemId)
  │     ├── calc.addItem(cart, itemId) → cart[itemId] + 1
  │     └── usage.recordUsage(itemId) → wx.Storage('usage_count')
  ├── onDecrease(itemId)
  │     └── calc.removeItem(cart, itemId) → cart[itemId] - 1, delete if 0
  ├── onRemoveItem(itemId) → delete cart[itemId]
  ├── onStartEditQty / onEndEditQty
  │     └── calc.setQuantity(cart, itemId, val) → 直接设置数量
  ├── refreshSelected()
  │     ├── calc.getTotal(cart, priceMap) → 合计金额
  │     ├── calc.getDetail(cart, priceMap) → 已选明细列表
  │     ├── calc.numberToChinese(total) → 中文大写金额
  │     └── calc.getSelectedCount(cart) → 已选项数
  ├── saveCart() → wx.Storage('current_cart')
  └── onCopyReport()
        ├── calc.formatReport(cart, priceMap) → 文本报告
        ├── wx.setClipboardData(report) → 复制到剪贴板
        └── history.saveHistory(cart, total, report) → wx.Storage('calc_history')
```

---

## 五、Storage 键值设计

| Key | 类型 | 说明 |
|-----|------|------|
| `current_cart` | `{ [itemId]: quantity }` | 当前购物车（页面加载时恢复） |
| `usage_count` | `{ [itemId]: count }` | 使用频次（每次+1操作累加） |
| `calc_history` | `[{ id, time, cart, total, report, itemCount }]` | 历史记录（最多50条） |

---

## 六、关键功能设计

### 6.1 搜索浮层

- 点击搜索框 → 展开浮层（dropdown-panel + dropdown-mask），显示全部物品按常用排序
- 输入关键词 → 实时过滤（匹配 name + spec）
- 点击物品 → `onQuickAdd` 直接 +1，收起浮层
- 点击遮罩或收起按钮 → 关闭浮层
- 浮层内显示 badge（已选数量 > 0 时）
- `catchtouchmove="preventMove"` 阻止遮罩下滚动穿透

### 6.2 已选清单

- 每项卡片：名称 + 规格 + 单价 + 数量选择器（- / 数字 / +）+ 小计 + 删除
- 点击数字 → 切换为 input（editingId 控制），自动全选（selection-start="0" selection-end="999"）
- 点击空白区域 → `onCancelEdit` 取消编辑
- blur/confirm → `onEndEditQty` 保存
- 删除按钮使用 `catchtap` 阻止事件冒泡

### 6.3 底部合计栏

- flex 列布局：`page { display: flex; flex-direction: column; height: 100vh }`
- `.main-content { flex: 1; min-height: 0 }` 自动填充剩余空间
- `.bottom-fixed-bar { flex-shrink: 0 }` 固定在底部不悬浮
- 显示：合计 ¥XXX + 中文大写金额 + 清空 + 复制清单按钮

### 6.4 中文大写金额

```js
numberToChinese(num)
// 0 → "零元整"
// 12580 → "壹万贰仟伍佰捌拾元整"
// 12580.5 → "壹万贰仟伍佰捌拾元伍角整"
// 支持 亿、万、仟、佰、拾、元、角、分
```

### 6.5 文本报告

```
路产损坏清单：三角形反光标志牌(边长130cm 高强级) 5块，波形梁护栏(三波板) 3米。路损金额合计：¥12,580.00元。
```

### 6.6 事件冒泡控制

| 元素 | 事件绑定 | 原因 |
|------|----------|------|
| 已选卡片内的按钮（+/-/删除） | `catchtap` | 阻止冒泡到 scroll-view |
| 搜索框下拉箭头 | `catchtap` | 阻止冒泡，独立切换浮层 |
| scroll-view | `bindtap="onCancelEdit"` | 点击空白取消编辑 |

---

## 七、开发过程中关键决策记录

### UI 迭代

1. 底部栏经历多轮调整：悬浮 → 固定 → 两行布局 → 单行 → 恢复两行 → 最终采用 flex 列布局固定底部
2. 搜索浮层从简单列表演进为：按使用频次排序 + 实时过滤 + 点击即添加收起
3. 物品数量编辑：从纯按钮 → 按钮+数字点击切换input → 自动全选 → 空白取消

### 数据设计

1. 最初设计为 1998/2020 切换，后改为全部合并（2020 价格覆盖同名项目）
2. 明细页最初独立存在，后合并到计算页直接展示
3. 中文大写金额从无到有，展示在合计数字下方

### 性能优化

- `app.json` 添加 `"lazyCodeLoading": "requiredComponents"` 启用组件按需注入

### 上架相关

- 用户隐私：选择"未采集用户隐私"（纯离线，所有数据仅存本地）
- 微信认证：未完成（不影响审核和发布，仅影响搜索和分享）
- 小程序介绍：已提供标准描述文案
- 版本描述：已提供 v1.0.0 功能点列表

---

## 八、工具函数清单

### calculator.js

| 函数 | 用途 |
|------|------|
| `buildPriceMap()` | 构建 { itemId: item } 索引 |
| `addItem(cart, itemId)` | 数量 +1 |
| `removeItem(cart, itemId)` | 数量 -1，0 则删除 |
| `setQuantity(cart, itemId, n)` | 直接设置数量 |
| `getSubtotal(id, qty, priceMap)` | 单项小计 |
| `getTotal(cart, priceMap)` | 总金额 |
| `getDetail(cart, priceMap)` | 明细数组 |
| `getSelectedCount(cart)` | 已选项数 |
| `formatReport(cart, priceMap)` | 文本报告 |
| `formatMoney(num)` | 千分位格式化 |
| `numberToChinese(num)` | 中文大写金额 |

### usage-tracker.js

| 函数 | 用途 |
|------|------|
| `recordUsage(itemId)` | 累加使用次数 |
| `getUsageCounts()` | 读取全部频次 |
| `sortByUsage(items)` | 按频次降序排列 |

### history.js

| 函数 | 用途 |
|------|------|
| `saveHistory(cart, total, report)` | 保存记录（最多50条） |
| `getHistory()` | 读取全部记录 |
| `deleteHistory(id)` | 删除单条 |
| `clearHistory()` | 清空全部 |

---

## 九、开发环境与部署

- 开发工具：微信开发者工具
- 运行方式：直接打开项目根目录，无需构建/安装
- 预览：开发者工具 → 预览 → 扫码真机体验
- 体验版：上传 → 公众平台选为体验版 → 添加体验成员 → 扫码
- 正式发布：上传 → 公众平台提交审核 → 审核通过 → 发布上线
- 基础库要求：≥ 2.11.1（组件按需注入需要）
